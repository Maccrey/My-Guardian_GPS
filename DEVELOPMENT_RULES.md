# 개발 규칙 (Development Rules)

## 1. 상태 관리 및 아키텍처

- GetX 패턴 사용 (상태관리, 라우팅, 의존성 주입)
- GetX 패키지 버전은 4.6.5를 사용 (4.6.6 이상에서 iOS 빌드 오류 발생)
- 서비스 클래스는 Get.put을 사용하여 전역적으로 등록
- 주요 서비스는 싱글톤 패턴 사용 (getInstance 메소드)
- 기능별로 분리된 서비스 구조 유지 (AuthService, LocationService 등)

## 2. 초기화 및 설정

- Firebase 초기화를 우선적으로 수행
- 로컬 데이터는 SharedPreferences를 사용하여 관리
- 환경변수는 .env 파일을 통해 관리
- 플랫폼별 분기 처리 시 kIsWeb 확인

## 3. 라우팅 및 페이지 관리

- 모든 화면은 GetPage로 라우트 등록
- 페이지 전환 시 Get.toNamed 사용
- 필요한 경우 Binding 사용하여 의존성 주입

## 4. 다국어 지원

- 국제화 지원 (한국어/영어)
- GlobalMaterialLocalizations 사용

## 5. 로깅 및 디버깅

- 오류 발생 시 상세 로깅 (debugPrint, FlutterError.dumpErrorToConsole)
- 디버그 로그 접두사 일관성 유지
  - ✅: 성공
  - ❌: 오류
  - ⚠️: 경고
  - 🚀: 초기화
  - 🔑: 인증 관련
  - 💬: 메시지 관련
  - 🗺️: 위치 관련
  - ☎️: 연락처 관련
  - 📡: 네트워크 관련

## 6. 코드 관리

- 코드 변경/비활성화 시 주석으로 기록 유지
- 중요 컴포넌트에는 주석으로 표시 (IMPORTANT 태그 사용)
- 미사용 코드는 주석 처리하되 삭제하지 않고 유지

## 7. 오류 처리

- try/catch 블록으로 예외 처리
- 예외 발생 시 스택 트레이스 함께 로깅
- 중요 서비스 초기화 실패 시 대체 동작 구현

## 8. UI/UX 가이드라인

- Material 3 디자인 사용
- 다크 모드 지원
- 시스템 설정에 따른 테마 적용

## 9. 보안

- Firebase 인증 사용
- 민감한 정보는 환경 변수로 관리
- API 키는 플랫폼별로 적절히 관리

## 10. 성능 최적화

- 위젯 재사용 및 메모리 관리
- 불필요한 리빌드 방지
- 이미지 캐싱 활용

## 11. MSA 개발 규칙

- 각 서비스는 독립적으로 개발 및 배포
- 서비스 간 통신은 API Gateway 패턴 적용
- 각 서비스는 자체 데이터베이스 사용

## 12. 지오펜싱 및 위치 알림 서비스 개발 규칙

- 위치 기반 알림(귀가알림 등)은 지오펜싱(Geofencing) 방식으로 구현
- GeofenceService는 집 위치(원형 영역) 등록 및 진입/이탈 이벤트만 담당
- HomeArrivalService는 지오펜싱 이벤트를 받아 메시지 생성/전송 트리거만 담당
- 메시지 데이터 구조는 Firestore에 JSON 문자열(content)로 저장하며, 위치/주소/메시지/타입/타임스탬프 등 포함 (예시는 README.md 참조)
- 서비스 간 의존성은 GetX DI로 관리, 각 서비스는 독립적으로 테스트 가능해야 함
- 이벤트 기반 아키텍처(콜백, 스트림 등)로 서비스 간 결합도 최소화
